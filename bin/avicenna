#!/usr/bin/env bash
####################################################################################################

# bin/avicenna – Avicenna unified CLI
set -euo pipefail

####################################################################################################

AVICENNA_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

####################################################################################################

exec julia --project="$PWD" -e "
    push!(LOAD_PATH, \"$AVICENNA_ROOT/src\")
    include(\"$AVICENNA_ROOT/src/CLI.jl\")
    using .CLI
    CLI.dispatcher(ARGS)
" -- "$@"

####################################################################################################
